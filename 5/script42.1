#!/bin/bash

# 1. Создайте командный файл, который при выполнении должен
# получить не менее двух параметров. Если количество переданных
# сценарию параметров равно двум, то на экране появляются их значения,
# в противном случае – выведите имя сценария и требование
# задать два параметра.

if [ $# -lt 2 ]; then 
    echo "$0 принимает как минимум 2 параметра"
else
    echo $1 $2
fi
