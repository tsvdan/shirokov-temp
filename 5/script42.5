#!/bin/bash

# 5. Создайте сценарий, который спрашивает имя файла и 
# определяет имеется ли файл с таким именем в 
# домашнем каталоге пользователя - /home/<имя пользователя>.



# директории, в том числе и ., .. - считаю за файлы
# т.е. опять-таки без дополнительных проверок

read fname

if [ $(ls -la /home/$USER | grep $fname | wc -l) -gt 0 ];
then
    echo "exists"
else
    echo "does not exist"
fi
