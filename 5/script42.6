#!/bin/bash

# 6. Создайте командный файл, в котором над двумя заданными
# числами осуществляются следующие действия: эти числа складываются и перемножаются.
# Затем в цикле на экране распечатываются строки со словом Hello!.
# Количество строк равно минимальному из заданных чисел. 



print_hello() {
    local i=1
    while [ $i -le $1 ]; 
    do
        (( i = $i + 1 ))
        echo "Hello!"
    done
}
#===========================

read a b

let  ab_sum=$a+$b
let ab_prod=$a*$b

if [ $ab_sum -le $ab_prod ];
then
    print_hello $ab_sum
else
    print_hello $ab_prod
fi
